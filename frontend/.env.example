# En desarrollo NO definas esto (usa el proxy de Vite hacia localhost:4000).
# En producción (Vercel), defínelo con la URL pública del backend en Render:
# VITE_API_URL=https://mmm-backend.onrender.com
VITE_API_URL=
